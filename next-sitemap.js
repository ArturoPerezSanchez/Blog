/** @type {import('next-sitemap').IConfig} */

module.exports = {
    siteUrl: process.env.SITE_URL || 'https://blog.arturops.com',
    generateRobotsTxt: true, // (optional)
     // Default transformation function
  }